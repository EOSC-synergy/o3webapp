version: '3.7'
services:
    o3webapp:
        build:
            context: .
            target: production
        ports:
            - 3000:3000
        image: o3webapp
        container_name: o3webapp
