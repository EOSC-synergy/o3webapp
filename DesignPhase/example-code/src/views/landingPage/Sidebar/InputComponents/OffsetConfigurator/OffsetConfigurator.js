import React, { useState } from "react";
import ReferenceYearSlider from "../ReferenceYearSlider/ReferenceYearSlider";

export default function ReferenceModelSelector(props) {
    
    props.defaultReferenceModel;

    return (
        <>
            <ReferenceYearSlider />
            <button />
        </>
    );
}