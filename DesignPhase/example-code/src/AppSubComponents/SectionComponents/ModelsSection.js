import * as React from 'react';
import {Button} from './BasicComponents'

export default function ModelsSection() {

    const click = () => {
        return "TODO";
    }

    return (<div>
        <Button text="Add Model Group" onClick={click} />
    </div>);
}